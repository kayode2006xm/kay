# SERVER SETTINGS
server.port=8080

# FILE UPLOAD SETTINGS
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# SECURITY (Basic auth user for live)
spring.security.user.name=admin
spring.security.user.password=OLOWOESA
spring.security.user.roles=USER

# MARKET DATA API SETTINGS (Twelve Data)
marketdata.api.baseurl=https://api.twelvedata.com
marketdata.api.key=ff12565c54ed42cda376b58ea5fce1e1

# PATTERN EXTERNAL API SETTINGS (if used)
pattern.api.baseurl=
pattern.api.key=ff12565c54ed42cda376b58ea5fce1e1

# LOGGING (Optional: make logs more verbose for debugging)
logging.level.org.springframework=INFO

# STATIC RESOURCE MAPPING (if serving uploads)
spring.web.resources.static-locations=classpath:/static/,file:uploads/

# (Optional) Cross-Origin Resource Sharing, for separate frontend server
# spring.web.cors.allowed-origins=http://localhost:3000
